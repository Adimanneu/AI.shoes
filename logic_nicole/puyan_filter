"use strict";

function filter_puyan(unfilteredArray, minPrice, maxPrice) {
    let filteredArray = [SHOES];
    for (let shoe of unfilteredArray) {
        if (shoe.price >= minPrice && shoe.price <= maxPrice) {
            filteredArray.push(shoe);
        }
    }
    return filteredArray;
}